# Logging
output_folder=/home/ayu/Games
autostart_log=0
log_duration=0
log_interval=0
log_versioning

# Display — keep defaults (gpu_stats, cpu_stats, fps, frametime, throttling_status, text_outline)
# and add driver info + detailed frametime graph
gpu_name
vulkan_driver
engine_version
frame_timing_detailed
