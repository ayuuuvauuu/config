#!/bin/sh
watch -n 2 'for d in /sys/bus/usb/devices/*/; do
  c=$(cat "$d/power/control" 2>/dev/null)
  s=$(cat "$d/power/runtime_status" 2>/dev/null)
  p=$(cat "$d/product" 2>/dev/null)
  [ -n "$c" ] && printf "%-6s %-12s %s\n" "$c" "$s" "$p"
done'
